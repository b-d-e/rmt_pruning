{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Train with default config",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/train.py",
            "args": [
                "fit",
                "--config",
                "config/default.yaml"
            ],
            "console": "integratedTerminal"
        }
    ]
}